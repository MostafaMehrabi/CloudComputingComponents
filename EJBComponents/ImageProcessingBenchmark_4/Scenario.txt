Six images of different sizes are processed one by one, not in a pipeline, in order to inspect and compare the behavior framework in local and remote modes with respect to different sizes of workload. Here we only try the maximum resource allocation with 8 and 16 threads.This is especially to inspect the amount of workload for which operation offloading to cloud is and is not worth the transmission overhead of data.